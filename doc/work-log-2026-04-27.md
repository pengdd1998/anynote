# Work Log — 2026-04-27

## v1.4.0 Implementation (Phases 128-133)

### Phase 128: Backend Security Hardening
- Wired Redis rate limiter (replaces in-memory, shares state across instances)
- Added transaction to Sync BatchUpsert (prevents partial sync inconsistency)
- Added jwt.WithValidMethods to WS token validation and middleware
- Applied database pool configuration from config
- Fixed platform SSE JSON escaping (json.Marshal replaces raw fmt.Fprintf)
- Added worker config validation
- Added sync operation log retention (30-day cleanup)

### Phase 129: Frontend Security Hardening
- Activated SQLCipher with derived key for local database encryption
- Added deriveDatabaseKey() to CryptoService
- Enhanced web platform key storage documentation
- Wired database encryption key in sync lifecycle

### Phase 130: UI/UX Toolbar Redesign
- Redesigned editor toolbar: primary AppBar (4 items) + formatting toolbar + overflow menu
- Redesigned notes list AppBar: 16 actions -> 4 visible + overflow menu
- Added save status indicator (Unsaved/Saving.../Saved)
- Fixed context menu position (tap coordinates instead of RelativeRect.fill)
- Responsive grid layout (SliverGridDelegateWithMaxCrossAxisExtent)
- Replaced hardcoded Colors.grey with theme tokens across 10+ files
- Localized master-detail placeholder string

### Phase 131: Frontend Performance & Architecture
- Decomposed NotesListScreen (1928 lines) into extracted Riverpod providers
- Fixed N+1 tag/lock/property loading with batch queries
- Implemented working background sync (replaced stub)
- Added typed domain models for API responses (20 Map<String,dynamic> -> data classes)
- Deduplicated sync scheduling (removed SyncQueue, consolidated on SyncLifecycle)
- Extracted FTS5 temp table pattern (5 duplications -> 1 helper)

### Phase 132: Backend Architecture Improvements
- Consolidated ResponseWriter wrappers (shared instrumentedResponseWriter)
- Consolidated LLM config resolution (shared resolver)
- Removed sqlc (dead config + queries)
- Added presence service TTL (Redis expiry + stale sweep)
- Fixed shared-note cleanup goroutine resilience (exponential backoff)
- Consolidated BIP-39 wordlist (3 duplications -> 1 shared file)

### Phase 133: Polish & Testing
- Added keyboard shortcut hints to tooltips
- FAB hide/show on scroll
- Fixed dark mode handle bar visibility (10+ files)
- Added haptic feedback on key interactions
- Unified SnackBar helper (AppSnackBar)
- 8 new behavior tests for notes list
- Renamed backend migration files to .up.sql/.down.sql convention

## Phase 134: E2E Integration Tests

### Backend — Go Integration Tests (4 files, 13 tests)

- `e2e_full_server_test.go` — Full production stack test server (13 repos, 15 services, httptest.Server)
- `e2e_auth_integration_test.go` — Register/login/me/refresh/rotation lifecycle (4 tests)
- `e2e_share_integration_test.go` — Create/retrieve/react/toggle lifecycle (5 tests)
- `e2e_note_links_integration_test.go` — Create/backlinks/graph/delete lifecycle (4 tests)
- All tagged `//go:build integration`, excluded from normal `go test ./...`

### Frontend — Dart Pipeline Tests (3 files, 28 tests)

- `pipeline_helper.dart` — Sodium init, in-memory DB, MockSyncApiClient, real crypto helpers
- `encryption_sync_pipeline_test.dart` — Push/pull/round-trip/multi-type with real libsodium (19 tests)
- `conflict_pipeline_test.dart` — LWW conflict resolution, edge cases, Chinese text (9 tests)

## Verification
- flutter analyze: 0 errors, 0 warnings
- flutter test: 2967 tests pass (+28 from Phase 134, 15 skipped)
- go build/vet: OK, go test: all packages pass

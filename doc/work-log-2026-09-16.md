
### Semantic search E2E verified on device (bge-m3, 664e017 + fix)
User deployed bge-m3 on LAN Ollama. LlmConfig gained embeddingBaseUrl
(chat and embeddings can use different servers; device: GLM cloud chat +
LAN bge-m3 embeddings ' + '— '.encode() + b'the privacy-ideal shape). Full loop verified:
editor "Build semantic index" success snackbar (device ' + '→ '.encode() + b'LAN Ollama
embed ' + '→ '.encode() + b'vector-only upload to server), then advanced search semantic
toggle: query "spending" (zero lexical overlap ' + '— '.encode() + b'note says "budget")
recalled the note via pgvector cosine. Cross-language and paraphrase
recall confirmed working end to end.
Fixed en route: advanced-search entry was dead code (the ' + '⋯ '.encode() + b'menu is
hidden while the inline search is active, so its
if (_isSearching) advanced_search item could never appear); the inline
search bar now has a tune icon that carries the query into /search.
Also observed: expired access token 401-kicks to login mid-flow (fresh
login fixes); adb input text cannot type CJK (NullPointerException).

### G12 profiling + device QA round (fixes below, v2.8.6)
- Release cold start on Note 9 (am start -W, COLD): 1423 / 779 / 663 ms
  (first run includes post-install cold FS cache). Budget: first frame
  < 1.5 s on mid-range ' + '— '.encode() + b'MET (median 779 ms); native splash covers the
  window, no blank screen (login/home paint immediately). Debug build for
  contrast: 9.6 s cold start ' + '— '.encode() + b'release is ~12x faster to first frame.
- Device QA found and fixed three issues:
  1. PaperSurface showDoodle wrapped the content in Positioned.fill inside
     a Stack with no non-positioned children ' + '— '.encode() + b'the Stack expanded to
     infinite height under ListView/GridView constraints and killed the
     whole compose-tab layout (blank "AI 写作" page). Now the content is
     the non-positioned base child so the Stack sizes to it.
  2. The standalone publish screen (history + calendar tabs) had NO UI
     entry (only push-notification deep link). Added a calendar button on
     the compose screen "recent compositions" row.
  3. publishHistory(): server nil-slice serializes to JSON null, and the
     client cast crashed "Failed to load publish history" (empty history =
     the common case since /publish had no entry). Client now tolerates
     null (same as listPlatforms) AND the server GetHistory returns [].
- Verified on device: login/home/detail/editor menu (focus-mode dedupe
  live), analytics opt-in switch + view-stats sheet (0 events + clear),
  compose hero card renders, publish tabs switch, calendar grid with
  today ring + "0 posts this month" summary.

### Phase 131 calendar + analytics wrapper (v2.8.5)
- Content calendar: publish screen became a two-tab layout (publish /
  calendar). Hand-rolled month grid (no TableCalendar dep), today ring,
  solid dot on days with published posts (hollow for pending/failed),
  month summary line; tapping a day opens a sheet listing that day's
  posts with status/platform/time. l10n publicationsInMonth.
- core/analytics: opt-in, LOCAL-ONLY usage analytics (counts + durations,
  no content, one JSON blob in SharedPreferences, every storage error
  swallowed so analytics can never break a feature). Settings gains a
  usage-statistics section: opt-in switch + view-stats sheet (per-event
  counts, avg durations, clear button). Wired: note_created/note_updated +
  note_save duration, quick_capture_saved, search_performed,
  semantic_search, publish_started. l10n en/zh/ja/ko.
- Test hygiene: publish_screen_test scrolled the wrong Scrollable after
  the TabBar restructure (now scoped to the ListView); verified the
  remaining suite failures equal the pre-change baseline exactly (backup
  crypto / TTS / plan-screen env issues, untouched by this session).

### Phase 132 â semantic search + Phase 133 paper rollout (ac10ae4, then Phase 132 commits, v2.8.4)
- Phase 133: merged duplicate focus-mode menu entry (zen covers it);
  editor save now snapshots post-write so the timeline top entry labeled
  current is the live note; collection grid covers, compose hero card and
  the plan banner render on PaperSurface; G15 STRIPPED home_widget_service
  (+tests, zero references) and corrected COMPLETED labels; editor
  first-line handwriting stripped (quill 11.5 has no line-index style hook;
  font attribute would pollute synced Deltas and breaks CJK fallback).
- Phase 132: production postgres ALREADY runs pgvector/pgvector:pg16 (infra
  compose) ' + '— '.encode() + b'the risky image swap was a no-op. Migration 033 adds
  CREATE EXTENSION vector + note_embeddings (user_id+note_id unique, dim
  column instead of vector(N) typmod so any provider dimension works; seq
  scan per user is fine at this scale). CD deploy now takes a gzipped
  pg_dump snapshot before migrations (keeps 5). New /search/embeddings
  (PUT/GET/DELETE) + /search/semantic (POST, cosine distance, dim-filtered,
  k<=50, vector passed as text literal built from validated finite floats).
  Client: LlmConfig.embeddingModel (optional) wired through store/dialogs;
  LlmDirectClient.embeddings; editor menu action "Index note for semantic
  search" embeds the first 8000 chars and uploads the vector; search screen
  gains a Semantic toggle that embeds the query locally, queries the server
  by vector only, and resolves hits against local notes. l10n en/zh/ja/ko.

### CI recovery + XHS stats fetcher (d15de35, 90e86f3, v2.8.3)
- CI green again after two fixes: pubspec had flutter_native_splash
  in both deps and dev_deps (analyze --no-fatal-infos fails on
  warnings), and user_repository_integration_test.go had an unused
  variable since the recovery-key commit, breaking
  -tags=integration compilation. Coverage tests added for
  StatsRefreshHandler (threshold back to 50.1%).
- XHS adapter now implements StatsFetcher: chromedp loads the note
  with stored cookies and reads counters from __INITIAL_STATE__
  (DOM fallbacks, Chinese numeral parsing). Hourly refresher keeps
  best snapshot; v2.8.3 deployed. Views stay 0 (XHS hides reads).

### Phase 131 — per-post engagement stats (8d7bb59, v2.8.2)
Backend: migration 032 adds publish_logs.platform_post_id (partial index
on platform+post_id) and a post_stats snapshot table (views/likes/
comments/fetched_at, indexed publish+fetched DESC). Publish job success
now persists the adapter's PlatformID. New optional StatsFetcher
interface on platform adapters; the worker registers an hourly
refresh:post_stats task that snapshots stats for the last 30 days of
published posts (keeps 30 snapshots per post). /publish/history and the
detail endpoint attach the latest snapshot. Repository SELECTs
COALESCE(platform_post_id,'') so pre-existing NULL rows still scan.
Frontend: publish history cards show views/likes/comments (compact
1.2k/3.4w formatting); detail sheet gains a combined stats row; l10n
keys statsViews/Likes/Comments in en/zh/ja/ko.
Note: no adapter implements StatsFetcher yet (XHS chromedp scrape is
follow-up work), so cards render stats only once an adapter does.

### Phase 129 device-verified end-to-end
New note first save fired AutoTagger; 4 chips (#budget/#mobile-app/
#marketing/#coffee) appeared in the editor above the status bar.
Tapping #budget removed it and applied the tag — the home card now
shows the budget badge and a filter chip "budget" exists on home.

### Phase 128a — quick capture image attachments (d50394b)
Image button (badge count) in the quick-capture toolbar; picker
(gallery/camera) -> ImageStorage (compressed) -> staged thumbnails with
remove; save appends markdown refs so embeds render in editor/detail;
save fires on image even with no text. Enter point: launcher
shortcut /quick-capture route (FAB opens the full editor instead).

Remaining Sprint B: 128b URL snippet, 128c voice-to-text, G12 startup
profiling.

### Phase 128b/c — voice dictation + URL snippet (952f034)
- speech_to_text 7.4: mic button in quick-capture toolbar toggles
  dictation (zh_CN, interim results into the field, auto-commit on
  done). RECORD_AUDIO permission added. Missing-service state shows a
  snack and disables the mic.
- Bare-URL capture: a whole-capture https?://URL auto-expands to
  [title](url) with the client-fetched <title> (5s timeout, no server).

### G12 first step — native splash (4c1b946)
flutter_native_splash with warm cream (#FAF8F5) / dark navy (#1A1E2E)
launch backgrounds: cold start shows brand color instead of a blank
screen. Full TTI profiling still pending (Sprint B/G12 remainder).
Also: zen labels renamed to 沉浸模式 in zh, removing the duplicate
专注模式 confusion with focus mode (G11 item).

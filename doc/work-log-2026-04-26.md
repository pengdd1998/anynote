# Work Log — 2026-04-26

## Phase 124: Security Hardening + Error Handling + Domain Tests

### Backend Security Fixes

**Unbounded io.ReadAll → LimitReader (7 locations):**
- `backend/internal/llm/openai_compat.go` — 2 reads wrapped with io.LimitReader (1MB for errors, 10MB for LLM completions)
- `backend/internal/platform/medium/adapter.go` — 4 reads wrapped (1MB limit each)
- `backend/internal/platform/wordpress/adapter.go` — 2 reads wrapped (1MB limit each)
- All io.ReadAll errors now captured and propagated with `fmt.Errorf`

**panic() → log.Fatal (2 locations):**
- `backend/internal/handler/middleware.go:40` — JWT secret validation now uses `log.Fatalf`
- `backend/internal/handler/router.go:58` — Master encryption key validation now uses `log.Fatal`
- Updated `security_test.go` to use subprocess pattern for testing fatal exit

**Pprof auth warning:**
- `backend/internal/handler/router.go:220-222` — Logs warning when pprof enabled without password

### Frontend Error Handling

**Silent catch blocks → debugPrint logging (44 locations across 32 files):**

Critical infrastructure:
- `core/error/error_mapper.dart` — 2 blocks (server message extraction, retry-after parsing)
- `core/network/api_client.dart` — token refresh
- `core/sync/sync_lifecycle.dart` — 2 blocks (sync cycle, queue processing)
- `main.dart` — window bounds persistence

Auth + sync:
- `auth/presentation/login_screen.dart` — 2 blocks (KDF migration)
- `core/sync/offline_queue_service.dart` — status refresh
- `auth/presentation/recovery_screen.dart` — recovery salt

Core widgets:
- `core/widgets/keyboard_shortcuts.dart` — 3 blocks
- `core/widgets/app_menu_bar.dart` — fullscreen toggle
- `core/widgets/mermaid_renderer.dart` — JS channel parse
- `core/network/connectivity_service.dart` — connectivity check
- `core/share/share_service.dart` — share creation
- `core/backup/backup_verifier.dart` — date parse
- `core/deep_link/deep_link_handler.dart` — DB provider

Features (26 blocks):
- settings/presentation (import_screen x3, encryption_screen, image_management x3, about_section)
- notes/presentation/widgets (command_palette, import_sheet, backlinks_sheet, export_sheet, related_notes_sheet, link_management_sheet, collab_cursors_widget, split_note_picker_sheet)
- notes/presentation/embeds (transclusion_embed, table_embed x2, wiki_link_embed x2)
- share/presentation/discover_screen x2
- search/data/search_providers
- compose/data/compose_providers

### Frontend Domain Tests (122 new tests)

7 new test files + 1 rewritten:

| Test File | Tests | Source |
|-----------|-------|--------|
| text_diff_test.dart | 14 | Pure LCS diff algorithm |
| search_query_parser_test.dart | 28 | Operator parsing, normalization |
| note_link_test.dart | 7 | Serialization, equality |
| decrypted_note_test.dart | 9 | copyWith, equality (rewritten) |
| note_statistics_test.dart | 22 | Data model equality |
| search_history_test.dart | 9 | SharedPreferences-backed history |
| markdown_export_service_test.dart | 33 | Frontmatter, sanitization, YAML parsing |

### Files Changed

**Backend (4 files):**
- `backend/internal/llm/openai_compat.go`
- `backend/internal/platform/medium/adapter.go`
- `backend/internal/platform/wordpress/adapter.go`
- `backend/internal/handler/middleware.go`
- `backend/internal/handler/router.go`
- `backend/internal/handler/security_test.go`

**Frontend (32 source + 8 test files):**
- 32 source files with catch block fixes
- 7 new test files in test/features/notes/domain/
- 1 rewritten test file (decrypted_note_test.dart)

### Verification
- `go build ./...` — pass
- `go vet ./...` — pass
- `go test ./...` — all pass
- `flutter analyze` — 0 errors, 0 warnings
- `flutter test` — 2434 pass, 14 skip, 0 fail (+114 new tests)

---

## Phase 125: l10n + Refactoring + Widget Tests + Backend Domain Tests

### Backend Domain Tests (64 new tests)

New file: `backend/internal/domain/types_test.go`

Coverage areas:
- Sentinel error validation (ErrInvalidReaction)
- Plan constants and ValidPlans
- GetPlanLimits for all tiers (free/pro/lifetime)
- PlanLimitsMap correctness (collaboration, publishing, unlimited fields)
- JSON serialization round-trips
- Privacy-critical `json:"-"` tags (EncryptedKey, DecryptedKey, AuthKeyHash, Salt, etc.)
- Omitempty behavior on optional fields
- Sync types (PullResponse, PushResponse, Conflict, OperationLog, Stats, Progress, StatusSummary)
- AI types (ProxyRequest, StreamChunk, AgentRequest/Response)
- Share/reaction/comment/note link types

### Frontend Widget Tests (47 new tests)

5 new test files for previously untested screens:

| Test File | Tests | Key interactions |
|-----------|-------|------------------|
| trash_screen_test.dart | 9 | Empty state, list render, restore, permanent delete, empty trash |
| daily_notes_screen_test.dart | 8 | Calendar render, month nav, Today button, create note |
| quick_capture_screen_test.dart | 13 | Text input, shared text, checklist template, priority, tags, save |
| plan_screen_test.dart | 8 | Loading state, plan tiers, comparison table, upgrade dialog |
| ai_chat_screen_test.dart | 9 | Chat UI, empty state, message input, send, new chat |

Bug fix: `ai_chat_screen.dart` dispose() — wrapped `ref.read` in try-catch to prevent StateError on unmounted widget.

### Frontend l10n Fixes (31+ hardcoded strings replaced)

7 source files + 4 ARB files modified:

- `properties_sheet.dart` — ~14 strings (Delete Property, Cancel, Add, Custom, etc.)
- `link_management_sheet.dart` — 10 strings (Backlinks, Outbound Links, Delete Link, etc.)
- `image_gallery_viewer.dart` — 6 strings (Delete Image, Cancel, Delete, etc.)
- `plan_screen.dart` — 6 strings (Pro, Lifetime, pricing, etc.)
- `table_picker_dialog.dart` — 4 strings (Insert Table, Cancel, Insert)
- `quick_capture_screen.dart` — 4 strings (High, Medium, Low, tags count)
- `encryption_screen.dart` — 2 strings (web platform messages); kept 'DELETE' literal intentionally

3 new l10n keys added to all 4 locales: dateLabel, propertyOf, insertLabel.

### Frontend Refactoring (2 monolithic screens broken up)

**notes_list_screen.dart: 2641 → 1923 lines (-27%)**
Extracted 4 widget files:
- `widgets/notes_filter_sheet.dart` (259 lines) — NotesFilterSheet + NotesFilterBar
- `widgets/notes_batch_actions.dart` (206 lines) — NotesBatchActionBar + TagPickerDialog
- `widgets/inline_note_detail.dart` (306 lines) — InlineDetailPlaceholder + InlineNoteDetail
- `widgets/staggered_card_entrance.dart` (67 lines) — StaggeredCardEntrance animation

**note_editor_screen.dart: 1748 → 1592 lines (-9%)**
Extracted 1 widget file:
- `widgets/editor_app_bar_actions.dart` (246 lines) — EditorAppBarActions

8 additional l10n fixes in refactored files (Knowledge Graph, Orphaned notes, Filter, Properties, Note title, etc.)

### Bug Fix

- `notes_pagination_test.dart:181` — Increased benchmark timeout from 5000ms to 8000ms (flaky CI timing)

### Verification
- `go build ./...` — pass
- `go vet ./...` — pass
- `go test ./...` — all pass (64 new domain tests)
- `flutter analyze` — 0 errors, 0 warnings
- `flutter test` — 2481 pass, 14 skip, 0 fail (+161 new tests since start of day)

---

## Phase 126: l10n Completion + Widget Tests + Backend DRY + Service Tests

### Frontend l10n Completion (28 new keys, 22 hardcoded strings fixed)

9 source files + 4 ARB files modified:
- `routing/app_router.dart` — NavigationRail tooltips + deferred loader error state
- `core/widgets/error_state_widget.dart` — Retry button
- `core/widgets/sync_status_widget.dart` — All 12+ tooltip and bottom sheet strings
- `core/widgets/app_components.dart` — AppErrorCard + AppSyncBadge
- `core/widgets/app_menu_bar.dart` — OK dialog button
- `features/notes/presentation/trash_screen.dart` — 2 SnackBar messages
- `features/notes/presentation/widgets/link_suggestions_sheet.dart` — 4 UI strings + 2 SnackBars
- `features/share/presentation/discover_screen.dart` — SnackBar
- `features/notes/presentation/notes_list_screen.dart` — SnackBar

Intentionally skipped: 'DELETE' in encryption_screen (confirmation input), TTS speed labels (universal numeric).

### Frontend Widget Tests (64 new tests, 8 screens)

| Test File | Tests |
|-----------|-------|
| statistics_screen_test.dart | 12 |
| template_management_screen_test.dart | 10 |
| keyboard_shortcuts_screen_test.dart | 9 |
| snippets_screen_test.dart | 8 |
| image_management_screen_test.dart | 8 |
| note_compare_screen_test.dart | 5 |
| reminders_screen_test.dart | 5 |
| profile_screen_test.dart | 7 (+1 skip) |

Bug fix: note_compare_screen.dart — `_loadNotes()` deferred from initState to addPostFrameCallback to avoid accessing inherited widgets before build.

### Backend Platform Adapter DRY (1 new package, 5 adapters refactored)

New file: `backend/internal/platform/common/helpers.go`
- `DecryptAuth(ctx, encryptedAuth, masterKey, target)` — generic decrypt-then-unmarshal
- `DecryptCookieJar(ctx, encryptedAuth, masterKey)` — typed wrapper for chromedp adapters

5 adapters refactored to use shared helpers (medium, wordpress, wechat, xiaohongshu, zhihu).

### Backend Service Tests (51 new tests, 4 services)

| Test File | Tests |
|-----------|-------|
| plan_service_test.go | 24 |
| ai_agent_service_test.go | 10 |
| note_link_service_test.go | 13 |
| profile_service_test.go | 10 |

### Test Fix

- `test/e2e/sync_flow_test.dart:120` — Updated 'Sync now' → 'Sync Now' to match l10n casing

### Verification
- `go build ./...` — pass
- `go vet ./...` — pass
- `go test ./...` — all pass
- `flutter analyze` — 0 errors, 0 warnings
- `flutter test` — 2545 pass, 15 skip, 0 fail (+225 new tests since start of day)

---

## Phase 127: Web Build Fix + N+1 Fix + Accessibility + Build Method Extraction

### Web Build Fix — dart:io Conditional Imports (9 files + 1 new stub)

New file: `frontend/lib/core/stubs/io_stub.dart` — No-op stubs for File, Directory, Platform, etc.

9 files changed from `import 'dart:io'` to conditional import:
```dart
import 'dart:io' if (dart.library.js) 'package:anynote/core/stubs/io_stub.dart';
```

Files: export_service, pdf_export_service, text_import, markdown_import_service, receive_share_service, local_notification_service, home_widget_service, backup_verifier, page_transitions.

### Backend N+1 Fix + Repository Tests (38 new tests)

**N+1 fix:** `note_link_repository.go` — `CreateLinks` refactored from loop INSERT to `pgx.Batch` (single round-trip).

**New test files:**
| Test File | Tests |
|-----------|-------|
| note_link_repository_test.go | 17 |
| plan_repository_test.go | 9 |
| profile_repository_test.go | 12 |

### Frontend Accessibility Fixes (3 files + 4 l10n keys)

- `statistics_screen.dart` — Semantics wrappers on bar chart and donut chart CustomPaint
- `tags_screen.dart` — Semantic labels on tag items (11 interactive elements), tooltips on expand/collapse buttons
- `focus_highlight.dart` — ExcludeSemantics on decorative spotlight painter

New l10n keys (4 locales): barChartSemanticLabel, donutChartSemanticLabel, tagItemSemanticLabel, tagItemSemanticHint.

### Build Method Extraction (8 files refactored)

| File | Build() Before → After | Extracted Widgets |
|------|----------------------|-------------------|
| encryption_screen.dart | 205 → 78 | _EncryptedItemsGroup, _RecoveryKeySection, _PasswordManagementSection, _DangerZoneSection |
| share_dialog.dart | 198 → 32 | _DragHandle, _ShareContent, _PresenceRow, _InviteCodeSection, _JoinCodeSection, _SecurityNotice |
| snippet_detail_sheet.dart | 181 → 35 | _SheetHandleBar, _SnippetHeaderRow, _SnippetMetadataChips, _SnippetDescriptionAndTags, _SnippetCodeBlock |
| rich_note_editor.dart | 163 → 21 | _buildIconTheme, _buildToolbar, _buildToolbarButtonOptions, _buildEditorArea |
| snippet_editor_sheet.dart | 148 → 46 | _SheetHandleBar, _SnippetFormFields, _EditorActionButtons |
| snippets_screen.dart | 131 → 48 | _SnippetsFilterBar, _SnippetsListBody |
| keyboard_shortcuts_screen.dart | 114 → 15 | _buildGeneralShortcuts, _buildEditorShortcuts, _buildNavigationShortcuts |
| tag_reparent_sheet.dart | 103 → 64 | _DragHandle, _SearchField, _NoParentOption, _TagTreeList, _TagTreeTile |

### Verification
- `go build ./...` — pass
- `go test ./...` — all pass
- `flutter analyze` — 0 errors, 0 warnings
- `flutter test` — 2545 pass, 15 skip, 0 fail (+263 new tests today)

# Work Log — 2026-04-28

## v2.0.0 Implementation (Phases 135, 138)

### Phase 135: UX Polish — Error Handling & Offline Indicators

#### 135.1: Global Error Boundary
- Created `lib/core/widgets/error_boundary.dart` — StatefulWidget wrapping child widgets, catches build-time errors, shows recovery UI with Retry button, forwards errors to ErrorReporter

#### 135.2: Conflict Resolution UI
- Created `lib/features/notes/presentation/conflict_resolution_screen.dart` — Shows sync conflicts with Keep Local / Keep Server / Keep Both resolution options
- Added route `/sync/conflicts` in app_router.dart
- Added l10n keys: syncConflicts, noConflicts, conflictItem, serverVersion, keepLocal, keepServer, keepBoth

#### 135.3: Offline Indicator in Editor
- Added OfflineBanner to note_editor_screen.dart (hidden in zen mode)
- Added OfflineBanner to note_detail_screen.dart

#### 135.4: Unify SnackBar Usage
- Converted 37 files (~100+ raw ScaffoldMessenger.showSnackBar calls) to use AppSnackBar.info()/error()
- Only app_snackbar.dart (helper) and error_display.dart (custom UI utility) retain raw ScaffoldMessenger usage
- Encryption_screen.dart: 15 conversions (biggest single file)

#### 135.5: Notification Preferences Screen
- Created `lib/features/settings/data/notification_preferences.dart` — Data class + Riverpod Notifier with SharedPreferences persistence
- Created `lib/features/settings/presentation/notification_settings_screen.dart` — 4 toggle switches (Reminders, Sync Conflicts, Sharing, Push)
- Added route `/settings/notifications`, added tile in settings_screen.dart

#### 135.6: What's New Screen
- Created `lib/core/constants/changelog.dart` — Changelog class with kCurrentVersion and entries map for v1.0.0-v2.0.0
- Created `lib/features/settings/presentation/whats_new_screen.dart` — Dialog with version highlights and "Got it!" button
- Modified main.dart to show dialog on version update

#### 135.7: Onboarding Permission Requests
- Modified onboarding_screen.dart to request notification permissions via LocalNotificationService on "Get Started"

#### 135.8: Accessibility Audit
- Added tooltips to IconButtons missing them: ai_chat_screen, collection_detail_screen, llm_config_screen
- Most IconButtons already had tooltips from earlier phases

### Phase 138: Multi-Device Sync — Backend Device Identity

#### Backend (Go):
- Created migration 020_devices_table: devices table (id, user_id, device_id, device_name, platform, last_seen)
- Created migration 021_sync_blobs_device_id: added device_id column to sync_blobs
- Added Device struct to domain/types.go
- Created device_repository.go: RegisterDevice (upsert), ListDevices, DeleteDevice, UpdateLastSeen, CleanStaleDevices
- Created device_handler.go: POST /device-identity/register, GET /device-identity, DELETE /device-identity/{id}
- Added DeviceID to SyncBlob and SyncPushItem
- Updated sync_blob_repository.go: device_id in INSERT/SELECT queries
- Added account recovery endpoint: POST /auth/recover with RecoverRequest struct
- Added expired shared notes cleanup job (hourly via asynq scheduler)

#### Frontend (Dart):
- Added stable device ID generation/persistence in sync_engine.dart (UUID in SharedPreferences)
- Added resetDeviceIdCache() for testing
- Added deviceId field to SyncPushItem, passed in push()
- Added device management DTOs and API methods in api_client.dart: registerSyncDevice, listDevices, deleteDevice, recoverAccount

### Phase 135 Tests (~32 new):
- error_boundary_test.dart: 3 tests (child rendering, widget tree, StatefulWidget)
- changelog_test.dart: 5 tests (version format, entries validation)
- device_id_test.dart: 7 tests (UUID generation, persistence, caching, SyncPushItem serialization)
- conflict_resolution_test.dart: 10 tests (empty state, conflict cards, resolution buttons, multiple conflicts)
- notification_preferences_test.dart: 7 tests (defaults, serialization, equality, persistence)

## Verification
- flutter analyze: 0 errors, 0 warnings
- flutter test: pending (running full suite)
- go build/vet: OK

---

## Phases 136, 139, 140 Implementation

### Phase 136: UX Polish — Editor Experience
- 136.1: Fixed autosave debounce to use `AppDurations.autoSaveDelay` constant (was hardcoded 2s)
- 136.2: Enhanced save status indicator in AppBar (_AppBarSaveStatus widget showing saving/saved/unsaved)
- 136.3: Extended toolbar — added code block, checklist, indent/outdent, undo/redo buttons
- 136.4: Find & Replace bar (Ctrl+F) with search, match count, replace/replace all, CollabNotifier key
- 136.5: 12 new l10n strings for find/replace and new toolbar buttons
- Tests: 110 new (find_replace_bar, formatting_toolbar_extended, editor_autosave, FindReplaceController logic)

### Phase 139.1-2: CRDT Site Persistence
- Persistent CRDT siteIds — UUID v4 stored in SharedPreferences, cached in-memory
- Wired CRDT state persistence — load on joinRoom, periodic 5s save, save on leaveRoom
- MergeEngine exportState/loadState for full document serialization
- Tests: 21 new (collab_site_id, collab_persistence, MergeEngine serialization)

### Phase 139.3: Collab Backend Invite Codes
- Migration 022: collab_rooms + collab_room_members tables
- CollabRepository: 9 methods (CreateRoom, GetRoomByInviteCode, AddMember, etc.)
- CollabService: 8 sentinel errors, invite code generation (8-char, unambiguous chars)
- CollabHandler: 5 endpoints (POST /collab/rooms, /join, /leave, GET /members, /)
- Tests: 66 new backend (29 service + 37 handler)

### Phase 140.1: Image Compression
- Created image_compressor.dart — JPEG compression (1920px max, 85% quality) via flutter_image_compress
- Modified image_storage.dart — auto-compress images > 100KB before save
- Created NoteImages Drift table (id, noteId, path, hash, fileSize, width, height, isSynced)
- Created images_dao.dart — CRUD + getUnsyncedImages/markSynced for future sync
- Tests: 30 new (image_compressor, images_dao)

### Bug Fixes
- Fixed 14 collab_provider_test.dart failures caused by joinRoom() becoming async (SharedPreferences mock, database override, await on joinRoom calls)

## Final Verification
- Flutter: 3180 tests pass (0 failures, 15 skipped) — +176 new from Phases 136/139/140
- Backend: 14 packages, 66 new collab tests pass
- flutter analyze: 0 errors

---

## Phases 137, 140.2-3, 141 Implementation

### Phase 137: Cross-Platform Consistency
- 137.1: PlatformUtils utility (isDesktop, isMobile, isWeb, isApple, isTouchDevice, primaryModifierKey)
- 137.2: Breakpoints constants class (compact=600, expanded=1024)
- 137.3: Adaptive widgets (AdaptiveBuilder, AdaptiveVisibility, AdaptivePadding)
- 137.4: Focus management for desktop forms (FocusRing)
- 137.5: Desktop context menu widget (DesktopContextMenu)
- 137.6: Replaced all raw Platform.isX checks (12 files) with PlatformUtils getters
- 137.7: Keyboard shortcut consistency (platform-aware modifiers throughout)
- Tests: 112 new (platform_utils, adaptive_layout, keyboard_shortcuts, desktop_context_menu, focus_management)

### Phase 140.2-3: Image Sync + Web Images
- 140.2: Image push/pull in sync_engine.dart (encrypt images as sync blobs with item_type='image')
- 140.3: WebImageStorage (SharedPreferences-based, 5MB budget, base64 storage)
- image_storage.dart now conditionally delegates to WebImageStorage on web (kIsWeb)
- images_dao.dart: added upsertImage method
- Tests: 59 new (image_sync, web_image_storage)

### Phase 141: Collab Backend Hardening
- Migration 023: collab_operations table (room_id, site_id, clock, operation_type, payload)
- WS room access control: validates membership before allowing WS connection (403 if not member)
- CRDT operation persistence: each edit stored in collab_operations
- Reconnect catch-up: clients send ?since_clock=N, server sends missed ops as "catchup" messages
- CollabOpsStore and CollabMembershipChecker interfaces for testability
- Tests: 72 new backend (23 handler + 49 repository)

## Final Verification (all phases complete)
- Flutter: 3331 tests pass (0 failures, 15 skipped)
- Backend: 14 packages, all pass
- flutter analyze: 0 errors
- Database schema: v23 (collab_operations)

---

## Phases 142, 143 Implementation (v2.0.0 COMPLETE)

### Phase 142: Web Platform
- 142.1: CryptoFactory — unified crypto across all platforms (sodium_libs-based XChaCha20-Poly1305 everywhere)
- 142.2: DatabaseFactory — web executor documentation (OPFS/IndexedDB via drift_flutter)
- 142.3: UnsupportedError audit — only stubs remain (io_stub, web_download_stub, markdown_export_service)
- 142.4: Cross-platform crypto contract tests (key derivation hierarchy, encrypt/decrypt)
- Tests: 64 new (crypto_web_compat, database_factory, web_sync)

### Phase 143: Payment & Notification Infrastructure
- Migration 024: payments table (user_id, stripe_session_id, amount, status, plan)
- Migration 025: notifications table (user_id, type enum, title, body, data JSONB, is_read)
- PaymentRepository + PaymentService: checkout creation, webhook handling, HMAC-SHA256 signature verification
- NotificationRepository + NotificationService: CRUD, pagination, unread count
- PaymentHandler: POST /payments/checkout, GET /payments, POST /payments/webhook (public)
- NotificationHandler: GET /notifications, GET /notifications/unread-count, POST /notifications/{id}/read, POST /notifications/read-all
- Stripe integration with test mode fallback (graceful degradation without STRIPE_SECRET_KEY)
- Tests: 96 new backend (29 payment_service + 18 notification_service + 18 payment_handler + 22 notification_handler + 9 domain)

---

## v2.0.0 Final Verification
- Frontend: 3395 tests pass (0 failures, 15 skipped) — +428 new from v1.4.0
- Backend: 14 packages, ~934 tests pass — +234 new from v1.4.0
- flutter analyze: 0 errors (144 info-level only)
- go build/vet: clean
- Database schema: v25 (payments, notifications)
- All 9 phases (135-143) COMPLETE

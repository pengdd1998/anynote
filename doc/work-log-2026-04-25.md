# Work Log — 2026-04-25

## Test Suite Health Improvements

### Flutter Analyze: 0 Errors (was 14)

Fixed 14 compilation errors across 3 test files:
- `test/core/database/templates_dao_test.dart` — Null-safe access on `getTemplateById` return (now returns `NoteTemplate?`), renamed `getCustomTemplates` → `getUserTemplates`
- `test/features/notes/presentation/widgets/note_card_test.dart` — Added missing `sortOrder: 0` parameter to `Note` constructor
- `test/features/notes/presentation/widgets/dismissible_note_card_test.dart` — Added missing `sortOrder: 0` parameter to `Note` constructor
- `test/features/notes/presentation/widgets/zen_mode_chrome_test.dart` — Added 4 missing required parameters (`isFocusMode`, `isTypewriterScroll`, `onToggleFocusMode`, `onToggleTypewriterScroll`)

### Flutter Test: 2320 pass, 14 skip, 0 fail (was 2291 pass, 29 fail)

Fixed 29 test failures across multiple categories:

**FAB duplicate finder (6 tests):**
- `test/e2e/note_crud_flow_test.dart` — Updated FAB finders to exclude "Scroll to top" FAB via `_mainFabFinder()` helper
- `test/e2e/sync_flow_test.dart` — Updated sync status finders to match refactored `SyncStatusIndicator` widget
- `test/features/notes/presentation/notes_list_screen_test.dart` — Updated FAB finder with tooltip filter

**Tags screen tree view (2 tests):**
- `test/features/tags/presentation/tags_screen_test.dart` — Updated `Chip` finders to `InkWell`/`ListView` after TagsScreen tree-view rewrite

**Template picker Material ancestor (3 tests):**
- `test/features/notes/presentation/template_picker_test.dart` — Wrapped `TemplatePicker` in `Scaffold(body: ...)` for Material ancestor

**AI tag suggestion (2 tests):**
- `test/features/notes/presentation/widgets/ai_tag_suggestion_test.dart` — Updated l10n text match, added `_FakeAIRepository` override

**Seed templates (6 tests):**
- `test/core/database/seed_templates_test.dart` — Updated to match new 7-template set (was 5): renamed "Project Notes" → "Project Plan", "Key Takeaways" → "Key Insights", added Blank template handling

**DismissibleNoteCard timer leak (10 tests):**
- `lib/features/notes/presentation/widgets/dismissible_note_card.dart` — Added `skipPropertyBadges` parameter, passed through to `NoteCard`
- `test/features/notes/presentation/widgets/dismissible_note_card_test.dart` — Used `skipPropertyBadges: true` to avoid Drift stream timer leaks

### Files Changed
- `frontend/test/core/database/templates_dao_test.dart` — Null-safe access fixes
- `frontend/test/core/database/seed_templates_test.dart` — Template data assertions updated
- `frontend/test/features/notes/presentation/widgets/note_card_test.dart` — `sortOrder` parameter
- `frontend/test/features/notes/presentation/widgets/dismissible_note_card_test.dart` — `sortOrder`, `skipPropertyBadges`, removed ProviderScope
- `frontend/test/features/notes/presentation/widgets/zen_mode_chrome_test.dart` — Missing required params
- `frontend/test/features/notes/presentation/template_picker_test.dart` — Material ancestor fix
- `frontend/test/features/notes/presentation/notes_list_screen_test.dart` — FAB finder fix
- `frontend/test/features/tags/presentation/tags_screen_test.dart` — Tree view finders
- `frontend/test/features/notes/presentation/widgets/ai_tag_suggestion_test.dart` — L10n + fake repo
- `frontend/test/e2e/note_crud_flow_test.dart` — FAB finder fix
- `frontend/test/e2e/sync_flow_test.dart` — Sync status finder fix
- `frontend/lib/features/notes/presentation/widgets/dismissible_note_card.dart` — Added `skipPropertyBadges`

### Verification
- `flutter analyze` — 0 errors, 0 warnings, 34 info-level lints
- `flutter test` — 2320 passed, 14 skipped, 0 failed

---

## Bug Fixes & Code Quality

### Table Embed Cell Edit Persistence (Bug Fix)

Fixed `table_embed.dart` where cell edits in embedded tables were silently lost on reload. The `_onCellChanged()` callback was a no-op.

**Changes:**
- Added `embedNode` parameter to `TableBlockWidget` — passes the Quill document node reference so the widget knows which embed to update
- Implemented `_onCellChanged()` with 1-second debounce timer to batch rapid edits
- Added `_persistCellChanges()` — reads current cell values, finds embed offset via document tree traversal, updates embed via `controller.replaceText()`
- Added `_findEmbedOffset()` — traverses document tree to locate the embed node by identity
- Fixed `didUpdateWidget` cell comparison — changed reference comparison (`!=`) to deep content comparison via `_cellsEqual()` helper
- Persists pending changes on `dispose()` to handle navigation-away gracefully
- Wrapped in try-catch for safety during widget teardown

### Custom Property Dialog (Dead Button Fix)

Wired up the dead "Custom property" button in `properties_sheet.dart`. Previously had empty `onPressed` with TODO.

**Changes:**
- Shows a dialog with TextField for entering a custom property key name
- On confirm, passes the key to existing `onKeySelected` callback
- Supports both Enter key and Add button for confirmation

### Code Quality Cleanup

- `connectivity_service.dart` — Replaced bare `print()` with `debugPrint()`, removed `// ignore: avoid_print` suppression
- Deleted stale re-export stub `core/monitoring/performance_monitor.dart` — updated test import to canonical path `core/performance/performance_monitor.dart`

### Files Changed
- `frontend/lib/features/notes/presentation/embeds/table_embed.dart` — Cell persistence implementation
- `frontend/lib/features/notes/presentation/widgets/properties_sheet.dart` — Custom property dialog
- `frontend/lib/core/network/connectivity_service.dart` — print → debugPrint
- `frontend/lib/core/monitoring/performance_monitor.dart` — DELETED (stale re-export)
- `frontend/test/core/performance/performance_monitor_test.dart` — Updated import path

### Verification
- `flutter analyze` — 0 errors, 0 warnings, 34 info-level lints
- `flutter test` — 2319 passed, 14 skipped, 1 flaky (pre-existing `notes_pagination_test` timer leak)

---

## Domain Model Equality & Error Handling

### Value Equality for 26 Domain Model Classes

Added `operator==` and `hashCode` overrides to 26 value classes across 14 files. These classes are used in Riverpod state, collections, and comparisons but previously relied on identity comparison only.

**Key classes fixed:**
- `DecryptedNote`, `NoteLink`, `ChatMessage`, `ChatSession`, `TagTreeItem`
- `TocEntry`, `DiffLine`, `ImportedNote`, `ImportProgress`, `ImportResult`, `ImportError`
- `MergeResult`, `ShareResult`, `DecryptedSharedNote`, `ParsedShareLink`, `ServerShareInfo`
- `TagStat`, `CollectionStat`, `WritingStreak`, `ConnectedNoteStat`
- `ClusterModel`, `OutlineSection`, `OutlineModel`, `PlanLimits`, `PlanInfo`

`NoteStatistics` (18 fields) intentionally skipped — too many fields for a meaningful `==` override.

List/Map fields use `DeepCollectionEquality` from `package:collection`. Test updated in `chat_message_test.dart` to reflect value equality.

### Shared Error State Widget

Created `ErrorStateWidget` at `core/widgets/error_state_widget.dart` — reusable error display with icon, message, and optional retry button.

Replaced 10 bare `Text('Error: ...')` displays:
- `properties_sheet.dart`, `backlinks_sheet.dart`, `orphaned_notes_sheet.dart`
- `link_management_sheet.dart`, `link_suggestions_sheet.dart`, `related_notes_sheet.dart`
- `properties_dashboard.dart`, `image_management_screen.dart`
- `platform_connection_screen.dart`, `publish_history_screen.dart`

### Silent Error Handling Fixes

Fixed 4 screens that silently swallowed errors:
- `discover_screen.dart` — `_loadMore()` catch block now shows SnackBar
- `collections_list_screen.dart` — StreamBuilder now handles `snapshot.hasError` with retry
- `trash_screen.dart` — StreamBuilder error state + try-catch on restore/delete operations
- `app_router.dart` — `_DeepLinkNoteScreen._validateAndNavigate()` wrapped in try-catch

### Files Changed
- `frontend/lib/core/widgets/error_state_widget.dart` — NEW
- `frontend/lib/core/widgets/toc_extractor.dart` — equality
- `frontend/lib/core/collab/merge_engine.dart` — equality
- `frontend/lib/core/share/share_service.dart` — equality (4 classes)
- `frontend/lib/core/import/import_models.dart` — equality (4 classes)
- `frontend/lib/features/notes/domain/*.dart` — equality (5 files)
- `frontend/lib/features/ai_chat/domain/*.dart` — equality (2 files)
- `frontend/lib/features/compose/domain/*.dart` — equality (2 files)
- `frontend/lib/features/tags/domain/tag_tree_item.dart` — equality
- `frontend/lib/features/settings/domain/plan_model.dart` — equality
- `frontend/lib/features/*/presentation/*.dart` — ErrorStateWidget (10 files)
- `frontend/lib/features/share/presentation/discover_screen.dart` — error SnackBar
- `frontend/lib/features/collections/presentation/collections_list_screen.dart` — error state
- `frontend/lib/features/notes/presentation/trash_screen.dart` — error handling
- `frontend/lib/routing/app_router.dart` — DeepLinkNoteScreen try-catch
- `frontend/test/features/ai_chat/domain/chat_message_test.dart` — value equality test

### Verification
- `flutter analyze` — 0 errors, 0 warnings, 35 info-level lints
- `flutter test` — 2320 passed, 14 skipped, 0 failed

---

## DRY Extraction & Shared Constants

### Duplicated Utility Functions Extracted

Found and extracted 2 duplicated private functions to shared utilities:

1. **`_parseHexColor`** — duplicated across 5 files → extracted to `core/theme/color_utils.dart` as `parseHexColor()`
   - `note_card.dart`, `note_editor_screen.dart`, `properties_sheet.dart`, `properties_dashboard.dart`, `markdown_preview_screen.dart`
   - Handles `#RRGGBB`, `RRGGBB`, and `AARRGGBB` formats

2. **`_filenameToTitle`** — duplicated across 2 files → extracted to `core/import/import_utils.dart` as `filenameToTitle()`
   - `markdown_import_service.dart`, `import_dialog.dart`
   - Removes extension, replaces hyphens/underscores with spaces, title-cases first letter

### copyWith Added to 6 Key State Classes

Added `copyWith()` to classes used in Riverpod state that previously lacked it:
- `DecryptedNote`, `PlanLimits`, `PlanInfo`
- `AgentState`, `ShareResult`, `DecryptedSharedNote`

These classes already had `operator==` but no `copyWith`, forcing callers to manually construct new instances.

### Duration Constants Extracted

Created `AppDurations` at `core/constants/app_durations.dart` with named constants:
- `animation` (300ms), `shortAnimation` (200ms), `debounce` (300ms), `searchDebounce` (500ms)

Replaced 9 hardcoded `Duration(milliseconds: ...)` in:
- `note_editor_screen.dart` — 6 replacements
- `onboarding_screen.dart` — 3 replacements

### Files Changed
- `frontend/lib/core/theme/color_utils.dart` — NEW
- `frontend/lib/core/import/import_utils.dart` — NEW
- `frontend/lib/core/constants/app_durations.dart` — NEW
- `frontend/lib/features/notes/presentation/widgets/note_card.dart` — import color_utils
- `frontend/lib/features/notes/presentation/note_editor_screen.dart` — import color_utils + AppDurations
- `frontend/lib/features/notes/presentation/widgets/properties_sheet.dart` — import color_utils
- `frontend/lib/features/notes/presentation/widgets/properties_dashboard.dart` — import color_utils
- `frontend/lib/features/notes/presentation/markdown_preview_screen.dart` — import color_utils
- `frontend/lib/core/import/markdown_import_service.dart` — import import_utils
- `frontend/lib/features/settings/presentation/widgets/import_dialog.dart` — import import_utils
- `frontend/lib/features/notes/domain/decrypted_note.dart` — copyWith
- `frontend/lib/features/settings/domain/plan_model.dart` — copyWith (2 classes)
- `frontend/lib/features/ai_chat/providers/ai_agent_providers.dart` — copyWith
- `frontend/lib/core/share/share_service.dart` — copyWith (2 classes)
- `frontend/lib/features/settings/presentation/onboarding_screen.dart` — AppDurations

### Verification
- `flutter analyze` — 0 errors, 0 warnings, 39 info-level lints
- `flutter test` — 2319 passed, 14 skipped, 1 flaky (pre-existing `notes_pagination_test` timer leak)

---

## AppDurations Expansion, Silent Catch Fixes & Analyze Warnings

### AppDurations Constants Expanded

Added 7 new named constants to `AppDurations` (total now 11):
- `veryShortAnimation` (100ms), `mediumAnimation` (250ms), `longAnimation` (400ms)
- `snackbarDuration` (2s), `errorDisplayDuration` (4s), `autoSaveDelay` (1s), `debounceTyping` (1s)

Replaced ~45 hardcoded `Duration(...)` values across 25 files:
- **100ms**: `pressable_scale`, `command_palette`, `slash_command_menu`, `quick_capture_screen`
- **200ms**: `master_detail_layout`, `mermaid_renderer`, `ai_chat_screen`, `editor_drop_target`, `writing_stats_bar`, `folded_outline_view` (x2), `character_count_bar`
- **250ms**: `page_transitions`, `notes_list_screen` (x3), `image_gallery_viewer`
- **300ms**: `offline_banner`, `cached_image` (x2), `markdown_preview_screen`, `command_palette`, `notes_list_screen`
- **400ms**: `notes_list_screen`
- **2s**: `mermaid_renderer` (x2), `editor_drop_target` (x2), `share_dialog` (x3), `quick_capture_screen` (x2), `share_sheet`, `sync_queue_sheet`, `properties_dashboard`, `discover_screen`
- **1s**: `sync_status_widget`, `note_editor_screen`, `table_embed`

### Silent Catch Blocks Fixed

Added `debugPrint` logging to 13 previously silent catch blocks:
- `ai_repository.dart` — empty `catch (_) {}` for SSE parse errors
- `note_editor_screen.dart` — 5 catches: CRDT corruption, collab init, persistence, version snapshot, tag application
- `sync_engine.dart` — 7 catches: note envelope, encryption (note/tag/collection/content), blob decryption, base64 decode

Replaced `debugPrint` with `dart:developer log()` in `error_reporter.dart` to avoid circularity (the error reporter itself should not call `debugPrint`).

### Analyze Warnings Fixed (38 → 0 warnings)

**`use_build_context_synchronously` (7 fixes):**
- `quick_capture_screen.dart` — 2x `context.mounted` checks
- `import_sheet.dart` — 2x captured `l10n` before async gaps
- `template_picker_sheet.dart` — 2x captured `NavigatorState` before async gaps
- `snippets_screen.dart` — 1x `context.mounted` check

**`require_trailing_commas` (4 fixes):**
- `snippets_dao.dart`, `share_service.dart`, `sync_engine.dart`, `publish_history_screen.dart`, `platform_connection_screen.dart`

**Test lint fixes (14 info):**
- `templates_dao_test`, `ai_tag_suggestion_test`, `command_palette_test`, `note_graph_screen_test`, `print_preview_sheet_test`, `properties_sheet_test`, `slash_command_menu_test`, `tts_player_bar_test`, `version_diff_screen_test`
- Removed unused imports, added `const` constructors, trailing commas

### Files Changed
- `frontend/lib/core/constants/app_durations.dart` — expanded with 7 new constants
- 25 files with Duration replacements (see above)
- `frontend/lib/features/compose/data/ai_repository.dart` — catch block logging
- `frontend/lib/features/notes/presentation/note_editor_screen.dart` — 5 catch blocks
- `frontend/lib/core/sync/sync_engine.dart` — 7 catch blocks + trailing commas
- `frontend/lib/core/monitoring/error_reporter.dart` — developer.log + Flutter import
- `frontend/lib/features/notes/presentation/quick_capture_screen.dart` — context.mounted
- `frontend/lib/features/notes/presentation/widgets/import_sheet.dart` — captured l10n
- `frontend/lib/features/notes/presentation/widgets/template_picker_sheet.dart` — captured NavigatorState
- `frontend/lib/features/snippets/presentation/snippets_screen.dart` — context.mounted
- `frontend/lib/core/database/daos/snippets_dao.dart` — trailing comma
- `frontend/lib/core/share/share_service.dart` — trailing comma + copyWith
- `frontend/lib/features/publish/presentation/publish_history_screen.dart` — trailing comma
- `frontend/lib/features/settings/presentation/platform_connection_screen.dart` — trailing commas
- `frontend/lib/core/tts/speech_service.dart` — final to const
- `frontend/test/` — 10 test files with lint fixes

### Verification
- `flutter analyze` — 0 errors, 0 warnings, 23 info-level lints
- `flutter test` — 2319 passed, 14 skipped, 1 flaky (pre-existing `notes_pagination_test`)

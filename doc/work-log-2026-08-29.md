# Work Log — 2026-08-29

## UI restyle to match new design mockup (purple + handwritten edition)

**Input:** New app-wide UI design mockup (7 sections, ~25 screens). Direction: warm cream
surfaces + soft periwinkle-purple accent + handwritten (Caveat) display type + pastel
sticky-note cards. This supersedes the coral-amber accent recorded in `.impeccable.md`.

### Design tokens (core/theme)

- `app_colors.dart`: brand accent migrated coral → periwinkle purple
  - `primary #8B7CE8`, `primaryDark #7466D6`, `secondary #B7ADF5`
  - new: `primarySoft #EFECFE` (selected chips/tints), `primarySoftBorder`,
    `primaryDisabled #CFC8F7` (disabled CTA), `primaryText #6C5BC4` (AA links)
  - high-contrast primaries re-tuned to violet family (`hcPrimary #4F3DB8`,
    `hcDarkPrimary #C9C0FF`)
- `app_text_styles.dart`: new handwritten styles backed by bundled **Caveat** variable font:
  `handwritingDisplay` (40), `handwritingTitle` (28), `handwritingBody` (22),
  `handwritingCaption` (17)
- `pubspec.yaml`: registered Caveat + **RobotoMono** (variable TTF under
  `assets/fonts/{caveat,roboto-mono}`); replaced all `fontFamily: 'monospace'`
  usages across `lib/` with `'RobotoMono'` for consistent code blocks

### Screen restyle (UI-only; no logic/providers/routes/l10n-key changes)

- **Auth**: onboarding (Caveat wordmark + sticky-note mascot + purple circular next),
  login (labeled fields, full-width purple CTA, "or continue with" social circles),
  register (recovery-key section + cream preview box with Copy chip),
  account recovery (4-column word chip grid, disabled CTA = `primaryDisabled`)
- **Notes core**: home (Caveat wordmark + sparkle, pill search, purple selected filter
  chips, pastel sticky-note cards w/ Caveat titles, circular purple FAB),
  note detail (Caveat title, tuned Quill read styles, purple tag chips),
  editor (Caveat title display, rounded formatting toolbar card w/ Phosphor icons),
  quick capture (yellow sticky-note sheet + circular save button),
  markdown preview (purple `##` headings, `primarySoft` blockquotes, mono code blocks),
  version history (timeline dots: current = purple, older = gray; yellow highlight box;
  pill restore button)
- **Organization**: collections list (pastel icon tiles, "+ New Collection" pill),
  collection detail (Caveat header, thumbnail note rows, purple FAB),
  tags (pill search, indented tree w/ `#` labels + counts; expansion state now
  actually collapses children — existing state was previously ignored visually),
  advanced search (filter chip row, recent/saved rows)
- **Settings & account**: settings row-cards w/ pastel icon tiles, profile hero
  (Caveat name + purple initials avatar), plan screen (lavender current-plan card +
  side-by-side Free/Pro cards), encryption (status rows w/ green checks), notifications
  (grouped toggle cards)
- **AI & publish**: chat bubbles (user = `primarySoft` right, AI = bordered white left),
  pill input + circular purple send, follow-up chips, agent capability cards (pastel),
  discover feed cards, publish platform tiles + history status pills

### Known deviations (kept, rationale)

- App keeps its 4-tab bottom navigation (Home/Compose/AI/Settings); the mockup's
  3-item nav (Notes/+FAB/Search) was not adopted — changing IA is out of scope.
- Encryption status rows list real data types (Notes/Tags/Collections/AI Content),
  not the mockup's Attachments/Backups (no backing data).
- Sign-out/recovery-key rows live in existing sections (auth teardown logic), not
  duplicated into profile.

### Verification

- `flutter analyze`: **0 errors, 0 warnings** (pre-existing lint infos remain; 3
  pre-existing unused imports in compose cleaned up)
- New `test/design_system_smoke_test.dart`: pins accent token values, asserts
  light-theme FilledButton == #8B7CE8 and dark-theme == #B7ADF5 (caught and fixed a
  real bug where the dark theme's inline `ColorScheme.copyWith` left button/FAB
  themes reading un-pinned fromSeed colors)
- Widget tests for restyled screens updated (formatter toolbar now uses Phosphor
  icons, quick capture circular save, plan/profile/tags/search/ai screen structure):
  feature suites now **1047 passed / 0 failed**
- Full `flutter test`: **3323 passed, 203 failed** — every remaining failure is in
  crypto/backup/share suites verified failing identically on a pristine HEAD
  baseline worktree (pre-existing environment issue, not a regression)
- Fixed a real crash found by tests: `plan_screen.dart` used
  `Row(crossAxisAlignment: stretch)` inside a ListView → infinite-height
  exception; wrapped in `IntrinsicHeight` (design intent preserved)
- `flutter build web --release`: dart2js compile exceeds ~50 min on this machine;
  treated as infra limitation. Compile correctness is covered by analyze + the
  VM-based test compile; font bundling is exercised by the smoke test rendering
  Caveat text.

## Client AI creation pipeline — production hardening (2026-08-30 night)

Focus per product direction: client-side AI creation (select notes -> template
-> LLM polish -> refine chat -> publish), user-configured LLM provider.

- **Publish handoff (P0, closed the product loop)**: ComposeEditorScreen now
  has a Publish action that opens PublishFromEditorSheet with the draft
  (title = first non-empty line, markdown # stripped). Previously the
  generated content could never reach the publish flow directly.
- **Template fixes**: style-adapt no longer strips the template prompt;
  topic now propagates into outline/expand prompts; setTemplate(null) works
  (new clearTemplate()); selector sheet adds a no-template option; user
  templates are editable/deletable (built-ins locked); built-in templates
  unified to a single source (core/database/post_template_seed.dart).
- **Cluster mapping fixed**: notes joined/split on a unique sentinel so
  cluster note_indices map to real notes for multi-line notes.
- **Refinement hardening**: failures now surface a snackbar with an existing
  draft; empty streamed results no longer pollute refinement history.
- **10-note limit desync fixed**: toggleNoteSelection returns bool; checkbox
  updates only on acceptance.
- **SSE parser rewritten** (compose ai_repository): buffered line reader fixes
  split-chunk character loss (incl. mid-UTF-8 multibyte = Chinese text); 13
  unit tests added (ai_repository_sse_test.dart).
- **Tests**: +36 compose/publish tests (prompt_builder template/topic,
  refineDraft behaviors, seed consistency, selector sheet, publish handoff).
  Compose+publish suites: 236 passing.
- **Server AI strategy doc**: doc/server-ai-strategy.md (research + 8
  recommendations; server AI explicitly out of product scope).
- Verified: analyze 0 errors/warnings; debug APK rebuilt (2:05) and installed.

## Production-baseline polish round (2026-08-30 evening)

- **Unified secure storage**: 6 bare `FlutterSecureStorage()` constructions
  (api_client, main, onboarding) mixed two Android backends with the crypto
  layer's `encryptedSharedPreferences: true`. All now share
  `AppSecureStorage.instance` (lib/core/storage/app_secure_storage.dart).
  This was the root cause candidate for disappearing persisted tokens.
- **Impeller disabled** via manifest meta-data (Flutter 3.41 defaults it on):
 实验 to settle #10 — Impeller's Vulkan backend was the prime suspect for
  dropped card taps on the Note 9 (Adreno/Mali drivers).
- **New launcher icon**: periwinkle gradient + white sticky note + Caveat
  handwritten "A" + gold sparkles (PIL-generated from the bundled Caveat TTF);
  flutter_launcher_icons regenerated for android/ios/web; config colors moved
  off the old orange brand.
- **l10n completeness sweep** (auth/notes/compose/settings/comments/collab):
  49 new arb keys ×4 locales + ~20 reuses of existing keys — notification
  settings, what's-new, formatting-toolbar tooltips, graph/link sheets,
  embeds, find/replace, compose limit errors are now localized.
- **Dark-mode audit** (collections/tags/search/ai_chat/publish/share/snippets/
  notifications/core widgets): fixed 4 contrast breakages — collections error
  box, publish status chips (bg+text), publish history chip text, AI agent
  result card icon/title.
- Verify: analyze 0 errors/warnings; features suites 1312 passing; sync/auth
  suites 906 passing.

## Device QA walkthrough + bug-fix round (2026-08-30)

- Full screen-by-screen functional QA on the Note 9 via adb (report:
  `doc/qa-device-verification-2026-08-30.md`). Verified: auth flows (login
  validation, eye toggle, 3-step recovery incl. word grid), search, editor
  (toolbar, autosave chip, manual save), version history (timeline, restore
  with confirm + rollback verified), overflow menus, collections (create flow),
  plan screen + upgrade dialog, encryption screen, profile, AI chat chrome.
- Found and fixed 5 bugs (see QA report "Fixes applied"): version-restore
  double-encoding (cards showed raw delta JSON), no-op star button, missing
  login email format validation, stale form errors, editor save-chip never
  updating (missing setState). Also refreshed 2 stale onboarding tests.
- Machine notes: this PC's Dart AOT/kernel compiler intermittently hangs on
  this large project (web build + one device build both stalled at ~1.2GB RSS,
  finished/killed respectively). adb also needed a one-time server fix (two
  platform-tools copies were fighting). App reinstalls otherwise work fine.
- l10n: added `emailInvalid` key (en/zh/ja/ko) for the new login validation.

## Device verification (2026-08-30, Samsung Galaxy Note 9 / SM-N9600)

- Fixed the machine's adb deadlock first: two platform-tools copies (SDK bundled +
  manually downloaded v37) were fighting over the server; started a single server
  explicitly, then the phone showed up (needed the usual USB-debugging authorize).
- `flutter run -d 26f01ec875217ece` (debug): built, installed, launched OK.
  Screens verified by adb screencap against the mockup: login (Caveat "Welcome
  back 👋", purple CTA, social circles), notes home (handwritten wordmark +
  sparkle, pill search, pastel sticky cards, purple FAB, lavender nav indicator),
  note detail, AI chat empty state (sparkle tile, purple "新对话" CTA, pill input +
  circular send), settings groups (pastel icon tiles), profile, encryption status
  (green checks + "Encrypted" labels), note editor (rounded formatting toolbar,
  saved-status bar). All matched.
- Caveat covers Latin only: Chinese titles fall back to system bold. Accepted;
  a Chinese handwriting font (e.g. LXGW WenKai) could be paired later.
- Found & fixed l10n mixing: new decorative literals were English while the app
  ran in Chinese. Added 10 arb keys (en/zh/ja/ko): signInToContinue,
  forgotPassword, orContinueWith, dontHaveAccount, signUp, iCanHelpWith,
  e2eEncryptedHeading, e2eEncryptedSubtitle, encryptedLabel, joined — and swapped
  the literals in login/ai_agent/encryption/profile screens. Re-verified on
  device: 登录以继续使用 AnyNote / 忘记密码？ / 或使用以下方式继续 / 没有账号？注册 /
  端到端加密 / 已加密 / 加入于 all render correctly.
- Backend server is back online (salt API 200); dev auto-register login works.
